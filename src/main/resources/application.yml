spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stock_simulator?useSSL=false&serverTimezone=UTC
    username: root
    password: Thilina
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
